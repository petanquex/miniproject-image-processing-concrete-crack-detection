# Tuning Journal — Concrete Crack Detection

บันทึกการทำงานรอบ **cleanup → tune parameters → re-evaluate**
ชุดข้อมูล: CrackForest (CFD) 118 ภาพ วัดผลระดับ pixel (Precision / Recall / IoU / Dice)

---

## 2026-09-14

### สถานะก่อนเริ่ม

- pipeline รันได้ครบ 118 ภาพ แต่คะแนนต่ำ ปัญหาหลักคือ **precision ต่ำ** (มี noise จากพื้นหลังหลุดเป็น "crack" เยอะ)
- ใน `data/` มีไฟล์ขยะค้างอยู่:
  - `data/_test_cf/` (ข้อมูลทดสอบที่เหลือค้าง)
  - ภาพสังเคราะห์ `sample.*` ที่ได้ Dice 0.91 ซึ่งดึงค่าเฉลี่ยขึ้นเกินจริง
- พารามิเตอร์ที่ใช้รอบแรกคือค่า default ใน commit `723e49d`

### 1. Cleanup

ลบไฟล์ต่อไปนี้:

| ไฟล์ / โฟลเดอร์ | เหตุผล |
|---|---|
| `data/_test_cf/` | ข้อมูลทดสอบที่เหลือค้าง |
| `data/raw/images/sample.jpg`, `data/raw/masks/sample.png` | ภาพสังเคราะห์ ทำให้ค่าเฉลี่ยสูงเกินจริง |
| `data/results/sample_mask.png`, `outputs/figures/sample_overlay.png` | output ของภาพสังเคราะห์ |

ผลคือ `data/raw/images` และ `data/raw/masks` เหลือภาพจริง 001–118 ครบ (มี `.gitkeep` ปนอยู่ ไม่กระทบ)

> **หมายเหตุเรื่องตัวเลข baseline:** ใน CLAUDE.md บันทึกไว้ว่า IoU ≈ 0.21 / Dice ≈ 0.32 ซึ่ง
> **รวม `sample` ไว้ด้วย** (แถว AVERAGE เดิมคือ P 0.2529, R 0.6016, IoU 0.2104, Dice 0.3251)
> ถ้าตัด `sample` ออกให้เหลือ 118 ภาพจริง baseline ที่ถูกต้องคือ **IoU 0.2051 / Dice 0.3201**
> (รันพารามิเตอร์ชุดเดิมซ้ำแล้วได้ตัวเลขนี้ตรงทุกหลัก)

### 2. Grid search (`src/tune.py`)

```powershell
python src\tune.py --images data\raw\images --masks data\raw\masks --subset 25 --apply
```

- ใช้ภาพ 25 ภาพที่สุ่มแบบกระจายเท่าๆ กัน (`np.linspace`) ทำ pre-processing ครั้งเดียวแล้วใช้ซ้ำทุก combo
- ช่วงค้นหา: 4 × 3 × 3 × 2 × 3 × 3 = **648 combinations** เรียงผลตาม mean Dice

| Parameter | ค่าที่ลอง |
|---|---|
| `block_size` | 25, 35, 51, 75 |
| `C` | 5, 10, 15 |
| `close_ksize` | 3, 5, 7 |
| `open_ksize` | 3, 5 |
| `min_area` | 50, 120, 250 |
| `min_aspect` | 2.0, 3.0, 4.0 |

**8 อันดับแรก (บน 25 ภาพที่ใช้จูน):**

| Dice | IoU | Prec | Rec | block | C | close | open | area | aspect |
|---|---|---|---|---|---|---|---|---|---|
| **0.5272** | **0.3798** | 0.5473 | 0.5900 | 35 | 15 | 3 | 3 | 50 | 3.0 |
| 0.5257 | 0.3742 | 0.4677 | 0.6735 | 35 | 10 | 3 | 3 | 120 | 3.0 |
| 0.5225 | 0.3677 | 0.4855 | 0.6470 | 51 | 15 | 3 | 3 | 50 | 3.0 |
| 0.5174 | 0.3672 | 0.5348 | 0.5772 | 51 | 15 | 3 | 3 | 50 | 4.0 |
| 0.5174 | 0.3726 | 0.4906 | 0.6095 | 51 | 15 | 3 | 3 | 120 | 3.0 |
| 0.5173 | 0.3611 | 0.4453 | 0.6968 | 51 | 10 | 3 | 3 | 250 | 3.0 |
| 0.5145 | 0.3677 | 0.4962 | 0.6110 | 25 | 10 | 3 | 3 | 120 | 2.0 |
| 0.5139 | 0.3702 | 0.5307 | 0.5723 | 25 | 10 | 3 | 3 | 120 | 3.0 |

สิ่งที่เห็นจากตาราง: **ทั้ง 8 อันดับใช้ `close_ksize = 3` และ `open_ksize = 3`** แปลว่า kernel ขนาดใหญ่ทำให้รอยร้าวบางๆ
พองหรือถูกลบหายไป ซึ่งเสียมากกว่าได้

ชุดที่ดีที่สุดถูกเขียนลง `outputs/tuned_params.json` และ `pipeline.py` โหลดไฟล์นี้อัตโนมัติ

### 3. Full evaluation (118 ภาพ)

```powershell
python main.py --input data\raw\images --gt data\raw\masks --csv outputs\metrics.csv
```

---

## เปรียบเทียบพารามิเตอร์: ก่อน vs หลัง

| Parameter | ก่อน (commit `723e49d`) | หลัง (tuned) | ความหมาย |
|---|---|---|---|
| `block_size` | 35 | 35 | เท่าเดิม |
| `C` | 10 | **15** | threshold เข้มขึ้น พิกเซลต้องมืดกว่าบริเวณรอบๆ มากขึ้นถึงนับเป็น crack → noise ลด |
| `close_ksize` | 5 | **3** | เชื่อมรอยแตกน้อยลง ไม่ไปรวม noise ที่อยู่ติดกันเป็นก้อน |
| `open_ksize` | 3 | 3 | เท่าเดิม |
| `min_area` | 60 | **50** | ผ่อนลงเล็กน้อย เก็บรอยร้าวสั้นๆ ไว้ได้ |
| `min_aspect` | 3.0 | 3.0 | เท่าเดิม |

## เปรียบเทียบผลลัพธ์: ก่อน vs หลัง (118 ภาพจริง ไม่รวม sample)

### ค่าเฉลี่ย (mean)

| Metric | ก่อน | หลัง | Δ |
|---|---|---|---|
| **Precision** | 0.2479 | **0.5349** | **+0.2870** (×2.16) |
| **Recall** | 0.5982 | 0.5194 | −0.0788 |
| **IoU** | 0.2051 | **0.3399** | **+0.1348** |
| **Dice** | 0.3201 | **0.4872** | **+0.1671** |

### ค่ามัธยฐาน (median)

| Metric | ก่อน | หลัง |
|---|---|---|
| Precision | 0.2534 | 0.5530 |
| Recall | 0.7104 | 0.5351 |
| IoU | 0.2159 | 0.3610 |
| Dice | 0.3551 | 0.5305 |

### รายภาพ

- Dice **ดีขึ้น 92 ภาพ** และแย่ลง 25 ภาพ (อีก 1 ภาพเท่าเดิม)
- ภาพที่ได้ Dice ≥ 0.5 เพิ่มจาก **15 → 67 ภาพ**

**ดีขึ้นมากที่สุด 5 ภาพ (Dice):**

| Image | ก่อน | หลัง |
|---|---|---|
| 066 | 0.0000 | 0.6687 |
| 074 | 0.0527 | 0.6903 |
| 073 | 0.0000 | 0.5466 |
| 112 | 0.0000 | 0.5462 |
| 031 | 0.0235 | 0.5533 |

**แย่ลงมากที่สุด 5 ภาพ (Dice):**

| Image | ก่อน | หลัง |
|---|---|---|
| 023 | 0.3510 | 0.0377 |
| 104 | 0.4776 | 0.2779 |
| 102 | 0.6248 | 0.4549 |
| 011 | 0.4407 | 0.2994 |
| 022 | 0.7284 | 0.6063 |

**Dice ต่ำสุดหลังจูน (ใช้เป็นตัวอย่าง failure case ในรายงานได้):**

| Image | Dice | Precision | Recall | อาการ |
|---|---|---|---|---|
| 065 | 0.0000 | 0.0000 | 0.0000 | ตรวจไม่เจอเลย |
| 042 | 0.0123 | 0.0283 | 0.0079 | ที่ตรวจเจอเกือบทั้งหมดไม่ใช่รอยร้าว |
| 023 | 0.0377 | 0.6102 | 0.0195 | ที่เจอส่วนใหญ่ถูก แต่หลุดเกือบทั้งเส้น (C=15 เข้มเกินสำหรับภาพนี้) |
| 084 | 0.1497 | 0.6659 | 0.0843 | อาการเดียวกับ 023 |
| 055 | 0.1536 | 0.2169 | 0.1189 | — |

---

## สรุปและข้อสังเกต

1. **ได้ผลตามเป้า:** precision เพิ่มกว่าเท่าตัว ส่วน recall ลดลงเพียง ~0.08 ทำให้ IoU/Dice ดีขึ้นชัดเจน
2. **สิ่งที่เปลี่ยนแล้วได้ผลที่สุด** คือ C ที่สูงขึ้นคู่กับ closing kernel ที่เล็กลง ทั้งสองอย่างช่วยลดพิกเซลพื้นหลังที่หลุดเป็น crack
3. **Trade-off:** ภาพที่รอยร้าวจางหรือ contrast ต่ำ (เช่น 023, 084) ถูกกรองทิ้งมากขึ้น
   ทำให้ precision สูงแต่ recall ตกหนัก
4. **Overfit เล็กน้อย:** บน 25 ภาพที่ใช้จูนได้ Dice 0.527 แต่ทั้ง 118 ภาพได้ 0.487
5. **ค่าที่ดีที่สุดชนขอบช่วงค้นหา:** `C = 15` คือค่าสูงสุดที่ลอง ส่วน `close_ksize = 3`, `open_ksize = 3`
   และ `min_area = 50` คือค่าต่ำสุดที่ลอง ถ้าขยายช่วงออกไปอาจได้ผลดีขึ้นอีก

## งานถัดไปที่แนะนำ

- [ ] ขยาย `GRID` ใน `src/tune.py` เช่น `C: 12–25`, `close_ksize: 1, 3`, `open_ksize: 1, 3`, `min_area: 20–60`
- [ ] จูนด้วย `--subset 0` (ทุกภาพ) หรือแยก train/test เพื่อลด overfit และให้ตัวเลขในรายงานน่าเชื่อถือ
- [ ] เปรียบเทียบกับ Otsu (`--method otsu`) เพื่ออ้างอิงกับ Dorafshan et al. (2016)
- [ ] ดู overlay ของภาพ 065, 042, 023, 084 เพื่ออธิบาย failure case ในรายงาน
- [ ] อัปเดตตัวเลข baseline/current ใน CLAUDE.md

---

## 2026-09-20 — รอบ 2: ขยาย grid + แยก train/test

### สิ่งที่แก้ก่อนเริ่ม

วัดความเร็วก่อนแล้วพบว่า 1 combination ใช้เวลาแค่ **0.54 วินาที** บนภาพทั้ง 118 ภาพ
(pre-process ครั้งเดียวแล้วใช้ซ้ำ) แปลว่าข้อจำกัดเรื่องเวลาที่ทำให้รอบแรกต้องจูนบน
subset 25 ภาพนั้นไม่มีอยู่จริง จึงปรับ `src/tune.py` สองเรื่อง:

1. **ขยาย `GRID` ทุกด้านที่ชนขอบในรอบแรก** — รวม 3600 combinations

   | Parameter | รอบ 1 | รอบ 2 |
   |---|---|---|
   | `block_size` | 25, 35, 51, 75 | 15, 25, 35, 51, 75 |
   | `C` | 5, 10, 15 | 10, 12, 15, 18, 21, 25 |
   | `close_ksize` | 3, 5, 7 | **1**, 3, 5 |
   | `open_ksize` | 3, 5 | **1**, 3 |
   | `min_area` | 50, 120, 250 | 10, 20, 35, 50, 80 |
   | `min_aspect` | 2.0, 3.0, 4.0 | **1.5**, 2.0, 3.0, 4.0 |

   `ksize = 1` คือ kernel ขนาด 1x1 ซึ่งไม่เปลี่ยนภาพเลย = "ข้าม step นี้"
   ใส่ไว้เพื่อให้ grid ตอบได้ว่า morphology จำเป็นจริงไหม

2. **เพิ่ม `--holdout`** — แบ่ง train/test แบบ stride (หยิบทุกภาพที่ N) ไม่ใช่ตัดครึ่งหน้า-หลัง
   เพื่อให้สองฝั่งกระจายทั้ง dataset เท่าๆ กัน ค้นหาบน train 59 ภาพ
   แล้ววัดผลบน test 59 ภาพที่ไม่เคยถูกใช้จูน

### ปัญหาที่เจอ: argmax ของ train เชื่อไม่ได้

grid แรก 3600 combinations ให้ผู้ชนะบน train Dice 0.5139 / held-out 0.5122 ดูดี
แต่ค่าที่ชนะยังชนขอบอยู่สองตัว (`min_area = 80` คือค่าสูงสุด, `min_aspect = 1.5` คือค่าต่ำสุด)
จึงยิง grid เสริมเฉพาะจุดนั้นอีก 384 combinations (`min_area` ถึง 250, `min_aspect` ถึง 1.0)

ผลคือค่าที่ **train ดีขึ้น แต่ held-out แย่ลง**:

| params | train Dice | held-out Dice |
|---|---|---|
| `51, 15, 3, 1, area 180, aspect 2.0` | **0.5257** | 0.4884 |
| `35, 18, 1, 1, area 80, aspect 1.5` | 0.5139 | 0.5122 |

นี่คือ overfitting ของขั้นตอน **การเลือกค่า** เอง: เมื่อลองเกือบ 4000 combinations
ตัวที่ได้คะแนน train สูงสุดมักเป็นตัวที่ "ฟลุก" กับภาพไม่กี่ภาพ ไม่ใช่ตัวที่ดีจริง

### วิธีแก้: เลือกแบบ 2 ขั้น ด้วยข้อมูล train ล้วน (`src/select_params.py`)

- **ขั้น 1** คัด 200-300 อันดับแรกตาม mean Dice บน train
- **ขั้น 2** แบ่ง train 59 ภาพเป็น 5 folds คำนวณ Dice ของแต่ละ fold
  แล้วให้คะแนน `mean - std` คือเลือกตัวที่ทั้ง**ดี**และ**นิ่ง** ไม่ใช่ตัวที่พีคเฉพาะบางภาพ
- **test 59 ภาพถูกแตะครั้งเดียว** ตอนรายงานผลตอนท้าย

ผลคือช่องว่าง train/test หายไปเกือบหมด (รอบ 1 ห่าง 0.040, รอบ 2 ห่าง 0.001)

### ผลข้างเคียงที่สำคัญ: grid บอกให้ปิด step 3 และ 4

ค่าที่ชนะแบบไม่บังคับอะไรเลยคือ `close_ksize = 1, open_ksize = 1, min_aspect = 1.0`
ซึ่งแปลว่า **morphology ทั้งก้อนและส่วน aspect ของ shape filter ถูกปิดทิ้ง** เหลือแค่ area filter
เหตุผลคือ `min_area` ทำหน้าที่กำจัด noise ซ้ำกับ opening อยู่แล้ว และรอยร้าวใน CrackForest บางมาก
opening กัดเส้นขาดมากกว่าช่วย

จึงวัดแบบบังคับเปิดแต่ละ step ด้วย protocol เดียวกัน เพื่อดูว่าแต่ละขั้นตอน "ราคา" เท่าไร:

| Variant | บังคับอะไร | Held-out Dice | ทั้ง 118 ภาพ | params |
|---|---|---|---|---|
| A | ไม่บังคับ | **0.5213** | **0.5205** | `35, 15, 1, 1, 120, 1.0` |
| B | morphology เปิด | 0.5003 | 0.4888 | `25, 12, 3, 3, 80, 1.5` |
| C | aspect filter เปิด | 0.5142 | 0.5090 | `25, 12, 1, 1, 80, 2.0` |
| D | เปิดครบ 4 step | 0.5068 | 0.4991 | `51, 15, 3, 3, 80, 3.0` |

**ตัดสินใจเลือก D** เพื่อรักษาโครงสร้าง 4 ขั้นตอนของโปรเจกต์และการแบ่งงานในทีมไว้
ต้นทุนคือ Dice ต่ำกว่า A อยู่ **0.0145** บน held-out ซึ่งถือว่าคุ้ม และตาราง A-D นี้
เอาไปใช้เป็น ablation study ในรายงานได้โดยตรง

### ผลสุดท้าย (params ชุด D)

`block_size = 51, C = 15, close_ksize = 3, open_ksize = 3, min_area = 80, min_aspect = 3.0`

| Metric | Baseline | รอบ 1 (118 ภาพ) | รอบ 2 (118 ภาพ) | รอบ 2 (held-out 59 ภาพ) |
|---|---|---|---|---|
| Precision | 0.2479 | **0.5349** | 0.4990 | 0.4822 |
| Recall | 0.5982 | 0.5194 | **0.5781** | 0.5994 |
| IoU | 0.2051 | 0.3399 | **0.3492** | 0.3546 |
| Dice | 0.3201 | 0.4872 | **0.4991** | 0.5068 |

- median Dice 0.5304 -> **0.5405**, median IoU 0.3609 -> **0.3703**
- เทียบรอบ 1 รายภาพ: ดีขึ้น 62 ภาพ แย่ลง 55 ภาพ เท่าเดิม 1 ภาพ (Dice >= 0.5: 67 -> 68 ภาพ)
- ลักษณะการเปลี่ยน: รอบ 2 **แลก precision ไป 0.036 เพื่อ recall 0.059** ซึ่งได้ IoU/Dice กลับมาดีกว่า
  (`block_size` ใหญ่ขึ้น 35 -> 51 ทำให้หน้าต่าง threshold ครอบบริบทกว้างขึ้น เห็นรอยร้าวต่อเนื่องขึ้น
  ส่วน `C` เท่าเดิมที่ 15 และใช้ `min_area` 50 -> 80 เป็นตัวตัด noise แทนการเพิ่ม threshold ให้เข้มขึ้น)

**ดีขึ้นมากที่สุด:** 085 (0.360 -> 0.660), 118 (0.156 -> 0.410), 105 (0.308 -> 0.502)
**แย่ลงมากที่สุด:** 021 (0.212 -> 0.000), 074 (0.690 -> 0.545), 052 (0.228 -> 0.086)

**Failure cases สำหรับรายงาน (Dice ต่ำสุดหลังรอบ 2):**

| Image | Dice | Precision | Recall | อาการ |
|---|---|---|---|---|
| 021 | 0.0000 | 0.0000 | 0.0000 | ตรวจไม่เจอเลย (รอบ 1 ได้ 0.212 — เป็นภาพที่ถอยหลัง) |
| 065 | 0.0000 | 0.0000 | 0.0000 | ตรวจไม่เจอเลย ทั้งสองรอบ |
| 042 | 0.0019 | 0.0033 | 0.0013 | ที่ตรวจเจอเกือบทั้งหมดไม่ใช่รอยร้าว |
| 023 | 0.0248 | 0.4128 | 0.0128 | ที่เจอส่วนใหญ่ถูก แต่หลุดเกือบทั้งเส้น |
| 052 | 0.0855 | 0.0744 | 0.1006 | รอบ 1 ได้ 0.228 |
| 097 | 0.1640 | 0.7641 | 0.0919 | precision สูงมากแต่ recall ต่ำ — เจอแค่ส่วนเดียวของรอยร้าว |

### Reproduce

```powershell
# 1) grid search หลัก (3600 combinations, ~16 นาที) + grid เสริมที่ขอบ
python src\tune.py --subset 0 --holdout 0.5 --csv outputs\tune_results_round2.csv

# 2) เลือกค่าสุดท้ายแบบ 2 ขั้น (variant full = เปิดครบ 4 step) แล้วเขียน tuned_params.json
python src\select_params.py --results outputs\tune_results_round2.csv outputs\tune_results_round2b.csv --variant full --apply

# 3) วัดผลเต็มทั้ง 118 ภาพ
python main.py --input data\raw\images --gt data\raw\masks --csv outputs\metrics.csv
```

เปลี่ยน `--variant` เป็น `any` / `morph` / `shape` เพื่อได้ตัวเลข A / B / C ในตาราง ablation

## งานถัดไป (อัปเดต)

- [x] ขยาย `GRID`
- [x] จูนด้วยทุกภาพ + แยก train/test
- [ ] เปรียบเทียบกับ Otsu (`--method otsu`) เพื่ออ้างอิงกับ Dorafshan et al. (2016)
- [ ] ดู overlay ของภาพ 021, 065, 042, 023 เพื่ออธิบาย failure case ในรายงาน
- [ ] 021 กับ 052 ถอยหลังจากรอบ 1 ชัดเจน ลองดูว่าเป็นเพราะ `block_size` ที่ใหญ่ขึ้นหรือ `min_area`
- [ ] ใส่ตาราง ablation A-D ลงรายงาน (เป็นหลักฐานว่าแต่ละ step ในไปป์ไลน์ให้ผลเท่าไร)
